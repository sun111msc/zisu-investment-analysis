# 11 大模块关系图

## 执行顺序

```
Stage 0 · HARD-GATE-MACRO-GATE
    └── 宏观情绪门禁（环境A/B/C/D → 总仓位上限）
         ↓
Stage 0.5 ⚡ · HARD-GATE-THEME-CLASSIFIER  ← v5.2 新增
    ├── 浑天·望远镜 —— 题材三问（级别+阶段+筹码）
    ├── 估值锚自动切换（PS / Forward PEG / PE+分红 / PB+周期）
    ├── 拥挤度>80% → 🛑 不碰 · <20% → ✅ 游资最爱
    └── 困境反转专属 → HARD-GATE-CYCLE-REVERSAL
         ↓
Stage 1 · 数据采集（22维并行）
    ├── 浑天·经纬 —— 6路并行采集
    ├── 浑天·司书 —— 研报解码+一致预期+PEAD       ← research-report-analysis
    └── 浑天·紫苏 —— 12步瓶颈深挖+紫苏叶分级         ← serenity
         ↓
Agent 分析
    ├── 浑天·朝议 —— 52评委多派系红蓝对抗           ← deep-analysis panel
    ├── 浑天·测影 —— PA入场点/止损位                ← price-action
    ├── 浑天·观流 —— 机构资金行为透视               ← institutional-flow
    ├── 浑天·潜渊 —— A股制度排雷+隐藏规则           ← ashare-insider
    ├── 浑天·推演 —— 量化策略交叉验证               ← Vibe-Trading
    └── 浑天·溯源 —— 困境反转与周期波动判定         ← CYCLE-REVERSAL
         ↓
Stage 2 · 决策
    ├── 浑天·司南 —— 模块冲突仲裁（三向裁决+一票否决）← HARD-GATE-ARBITER
    └── 浑天·弓矢 —— 风控执行闭环（6层：仓位/止损/执行/心理/组合/检查）← HARD-GATE-EXECUTION-LOOP
         ↓
        输出：完整投研报告（HTML+催化剂时间轴+仲裁徽章+52评委投票+执行计划）
```

## 模块间数据流转

```
MACRO-GATE ────→ 总仓位上限（约束所有买入信号）
       │
       ├──→ serenity（紫苏叶等级+治理评分）
       │         │
       │         ├──→ HARD-GATE-ARBITER（作为产业逻辑输入）
       │         └──→ HARD-GATE-EXECUTION-LOOP（作为仓位上限输入）
       │
       ├──→ price-action（PA入场点+止损位）
       │         │
       │         └──→ HARD-GATE-ARBITER（作为技术面输入）
       │
       ├──→ ashare-insider（制度排雷结果）
       │         │
       │         ├──→ HARD-GATE-ARBITER（作为制度风险输入，可触发一票否决）
       │         └──→ HARD-GATE-EXECUTION-LOOP（修正A股仓位上限）
       │
       ├──→ research-report-analysis（一致预期+研报共识度）
       │
       └──→ institutional-flow（机构资金行为）
                 │
                 └──→ HARD-GATE-EXECUTION-LOOP（确认仓/主升仓的条件验证）
```

## 一票否决权的三个输入源

```
HARD-GATE-ARBITER
  ├── MACRO-GATE（D级退潮 → 禁止一切交易）
  ├── ashare-insider（致命排雷 → 实控人清仓减持/审计非标/质押爆仓）
  └── price-action（趋势破坏 → 跌破核心OB+MA200+放量）
```
